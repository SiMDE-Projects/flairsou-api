import React from 'react';

import ContentWrapper from '../../UI/organisms/ContentWrapper/ContentWrapper';
import BookContent from '../../UI/organisms/BookContent/BookContent';

const Book = () => (
  <ContentWrapper content={<BookContent />} />
);

export default Book;
