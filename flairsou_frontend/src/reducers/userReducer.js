const initialState = {
  fetching: false,
  fetched: false,
  identified: false,
  user: null,
};

// TODO
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
